<?php
if(isset($_COOKIE['xhprof'])){
    if (extension_loaded('xhprof')) {
        include_once "<%= scope.lookupvar('webserver::params::php_xhprof_path') %>xhprof_lib/utils/xhprof_lib.php";
        include_once "<%= scope.lookupvar('webserver::params::php_xhprof_path') %>xhprof_lib/utils/xhprof_runs.php";
        xhprof_enable(XHPROF_FLAGS_CPU + XHPROF_FLAGS_MEMORY);
    }
}
